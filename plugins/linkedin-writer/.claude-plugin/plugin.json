{
  "name": "linkedin-writer",
  "description": "Conversational LinkedIn post generator. Run the `setup` agent first to establish brand context and post-type presets; then use the `linkedin-writer` agent to produce on-brand posts from a short brief. State lives in the plugin's persistent data directory so it works in Claude Cowork.",
  "version": "0.3.0",
  "license": "MIT"
}
